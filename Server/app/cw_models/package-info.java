/**
 * Contains typical model classes as in a MVC pattern.
 *
 * <p>Classes inside are data models that are supposed to
 * be held at the university's courseware. Since we do not
 * have access to a real courseware, classes here are all
 * similated ones.</p>
 */
package cw_models;